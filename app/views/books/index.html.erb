<h1> Books </h1>

<% @categories.each do |category| %>
    <h2><%= category.name %>:</h2>
  
    <% category.books.each do |book|  %>
        <%= link_to book_path(book) do %>
            <%= image_tag "#{book.img_url}", class: "img-link" %>
        <% end %>
    <% end %>
  

<% end %>








 